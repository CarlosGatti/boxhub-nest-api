<html>
  <head>
    <meta charset="UTF-8" />
    <style>
      .wrapper {
        width: 100%;
        background: #f7fafc;
        padding: 32px 0;
        font-family: Arial, sans-serif;
      }
      .content {
        max-width: 480px;
        margin: auto;
        background: #ffffff;
        padding: 32px;
        border-radius: 8px;
        text-align: center;
      }
      .logo {
        margin-bottom: 24px;
      }
      .logo img {
        max-width: 180px;
        height: auto;
      }
      .title {
        font-size: 24px;
        font-weight: bold;
        color: #2d3748;
        margin-bottom: 16px;
      }
      .subtitle {
        font-size: 16px;
        color: #4a5568;
        margin-bottom: 24px;
        line-height: 1.6;
      }
      .cta-button {
        display: inline-block;
        padding: 12px 24px;
        background: #3182ce;
        color: white;
        border-radius: 6px;
        text-decoration: none;
        font-weight: bold;
        margin-bottom: 24px;
      }
      .footer {
        margin-top: 32px;
        font-size: 12px;
        color: #4a5568;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <div class="content">
        <div class="logo">
          {{#if logoUrl}}
          <img
            src="{{logoUrl}}"
            alt="Discart-me Logo"
            style="max-width: 180px; height: auto;"
          />
          {{else}}
          <div style="font-size: 32px; font-weight: bold; color: #3182ce; margin-bottom: 24px;">
            Discart-me
          </div>
          {{/if}}
        </div>
        <div class="title">Verify Your Email Address</div>
        <div class="subtitle">
          Thank you for creating your Discart-me account. Click the button below to verify this email address and
          complete the registration process:
        </div>
        <a class="cta-button" href="{{verifyUrl}}?token={{token}}" target="_blank">
          Verify My Email
        </a>
        <p class="footer">
          © {{year}} Discart-me — Condo Marketplace
        </p>
      </div>
    </div>
  </body>
</html>
